groups:
  - name: opportunities_detection
    interval: 30s
    rules:
      # Alerte si nouvelles opportunités > 10% spread
      - alert: NewOpportunitySpike
        expr: |
          count(
            monps_opportunity_spread{spread_pct > 10}
          ) > 3
        for: 30s
        labels:
          severity: info
        annotations:
          summary: "🎯 Nouvelles opportunités détectées"
          description: "{{ $value }} opportunités avec spread > 10%"
